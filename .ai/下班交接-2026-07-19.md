# 下班交接 -- 2026-07-19

## 今日主题：Graphify 工具全局接入（非功能开发）

本轮工作不涉及业务代码，全部是**开发工具链/环境配置**：把本机已装的 Graphify（代码知识图谱工具）接入 ZCode，使其可跨项目使用。

---

## 今日完成

### 1. 调查现状
- 确认 Graphify 装在系统 Python 3.11：`/Library/Frameworks/Python.framework/Versions/3.11/bin/graphify` + `graphify-mcp`（pip 包名 `graphifyy` 0.9.20）
- 确认 ZCode 全局 MCP 配置位置：`~/.zcode/cli/config.json` → `mcp.servers`（原本已有 context7 / codegraph / deveco-mcp / antd）
- 确认项目既有 codegraph 工作流深度嵌入（AGENTS.md / package.json / MEMORY.md / PLAN.md 全部指向它）

### 2. 全局接入 Graphify（user scope，所有项目生效）
- ✅ 在 `~/.zcode/cli/config.json` 的 `mcp.servers` 新增 `graphify` server：
  ```json
  "graphify": { "type": "stdio", "command": "graphify-mcp", "args": [] }
  ```
- ✅ CLI 进 PATH：给 `graphify` / `graphify-mcp` 在 `~/.local/bin` 建 symlink（指向系统 Python 3.11 路径，与已工作的 `codegraph` symlink 同目录）

### 3. 本项目构建图谱（验证通路）
- 首次构建用 `graphify . --code-only`（无 LLM key，只做代码 AST 索引）
- 产物：`graphify-out/graph.json` = **333 节点 / 620 links / 21 社区**
- 验证 `graphify explain "deploy.sh"` 查询正常返回节点 + 连接关系
- ⚠️ `--code-only` 跳过了 11 docs + 5 images（需 LLM key 才能语义化，已列入后续任务）

### 4. `.gitignore` 维护
- ✅ 在根 `.gitignore` 新增 `/graphify-out/`（构建产物，不入库）
- ✅ `git check-ignore` 验证通过；`.codegraph/` 早已自带 ignore，无需动

### 5. codegraph vs graphify 取舍决策
- **结论：不迁移，两者分工并存**
  - **codegraph（主）**：代码导航、符号 FTS 检索、impact 影响分析 —— AGENTS.md/package.json/MEMORY.md 维持现状
  - **graphify（辅）**：架构全景、社区聚类报告、（后续）文档/图片语义化、跨库 merge
- 理由：codegraph 是既定稳定工作流（879 edges / FTS5 / impact 分析），graphify 独特价值在 codegraph 覆盖不到的聚类与文档语义化

---

## 已修改文件

```
 .gitignore                                          | 3 +++  （新增 /graphify-out/）
 .ai/下班交接-2026-07-19.md                          | (本文件，新增)
```

> `graphify-out/` 已被 .gitignore 忽略，不入库。
> `~/.zcode/cli/config.json` 和 `~/.local/bin/` 的 symlink 是**本机全局配置**，不在仓库内。

---

## 当前分支

- **分支:** master
- **最新 commit:** `d6fa181 feat: 强弱链图功能 + 游戏页布局统一`
- **工作区:** 干净（仅 `.gitignore` 待提交）

---

## 待办（后续方向）

### 本轮遗留
1. **重启 ZCode / 重载 MCP** —— 让 `mcp__graphify__*` 工具上线（重启后我即可 MCP 直查，不必走 Bash）
2. **文档/图片语义化** —— 配一个 LLM key（`OPENAI_API_KEY` / `DEEPSEEK_API_KEY` / `ANTHROPIC_API_KEY` 等）后重跑 `graphify .`（去掉 `--code-only`），把 11 docs + 5 images 纳入图谱

### 跨项目使用 Graphify（已就绪）
每个新项目只需两步：
```bash
cd /path/to/other-project
graphify .            # 首次构建（无子命令，直接传路径；纯代码用 --code-only 免 key）
# 然后在该项目目录下打开 ZCode 会话，MCP 按 cwd 读 graphify-out/graph.json
```
日常维护：`graphify update .`（增量）、`graphify watch .`（监听）

### 业务后续（沿用 PLAN.md）
- PWA Service Worker 调优
- 移动端响应式适配
- 游戏完成检测 + 自动提交记录到 API
- 题库初始数据填充

---

## 注意事项 / 已知问题

- **Bash 工具的非交互 shell 不加载 profile**：`which graphify` 在我这里会 "command not found"，但 ZCode 客户端和你的终端正常可用（PATH 含 `~/.local/bin` 和 Python 3.11 framework 路径）。我需要绝对路径 `/Library/Frameworks/Python.framework/Versions/3.11/bin/graphify` 才能跑。
- **graphify 与 codegraph 并存**：每个项目可能同时有 `.codegraph/` 和 `graphify-out/` 两份索引。本项目已定位分工，别同时维护。
- **AGENTS.md 未改动**：我的日常行为仍为"搜索优先 codegraph + 改后 `npm run s:sync`"。graphify 仅在你显式要求架构全景/聚类报告时使用。

---

## 接班命令

```bash
cd /Users/loho/Developer/mysudoku
npm run dev          # http://localhost:3003/game
# 题目管理: http://localhost:3003/puzzles （密码 mysudoku）

# Graphify（如需用）：
graphify explain "某符号"
graphify query "某个自然语言问题"

# 重启 ZCode 后，mcp__graphify__* 工具自动可用
```
