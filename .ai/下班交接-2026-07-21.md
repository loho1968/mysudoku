# 下班交接 -- 2026-07-21

## 今日主题：数独生成器 + 出题 UI + 序号字段 + 多项修复

### 今日完成

#### 一、核心功能：数独题目生成器（commit `99d4aec`）

**新增 `src/lib/sudoku/generator.ts`（~250 行）：**
- 随机化回溯生成合法 9×9 终盘
- 挖空保持唯一解（MRV 启发式回溯求解器，单题 <100ms）
- 复用项目已有 `applyAllTechniques` 反推解题技巧链
- 按给定数 + 使用技巧评定难度（1 简单 / 2 中等 / 3 困难）
- 支持 `difficulty` 或 `technique` 双模式筛选

**新增出题 API `POST /api/puzzles/generate`：**
- Body: `{difficulty|technique, count}` （count 上限 20）
- 权限：编辑模式（本地免密/远程密码）
- 写库时自动分配 seq

**序号字段 `seq`：**
- 数据库 puzzles 表加 seq 列（自动迁移，已有题按 created_at 补齐）
- 4 个 API route（list/all/upsert/import）返回或分配 seq
- PuzzleTable 加「序号」列；列表默认按 seq ASC 排序
- 游戏页显示「第 N 题」

**UI 出题按钮（PuzzleTable 工具栏）：**
- 难度 Select + 数量 InputNumber + 「出题」按钮
- 技巧 Select + 「出题」按钮

服务器实测：`POST /api/puzzles/generate -d '{"difficulty":2,"count":5}'` 一次产出 5 题，平均耗时 <1s/题。

#### 二、Bug 修复

| Commit | 问题 | 修复 |
|--------|------|------|
| `f47ae8c` | /puzzles 页面 hydration 报错 | `useEditMode` 初始 `isEditMode` 恒为 false，sessionStorage 读取推迟到 effect |
| `37781cc` | 候选数同数字高亮底色不突出 | 改为橙色背景（`#fa8c16`）+ 白字 + 加粗；亮暗双主题适配 |

### 当前状态

- **本地**：master `37781cc`，工作树干净，已推送
- **服务器**：HEAD 同步到 `37781cc`，build 通过，pm2 在线（id=54, uptime 47m, restart=107）
- **题库**：服务器共 8 题（含今日新生成的 5 题）
- **PM2**：mysudoku 端口 3003，公网 `http://110.42.225.37:3003`

### 部署踩坑

1. **GitHub fetch 间歇失败**：服务器到 github.com 的 HTTPS 连接多次超时（"Failure when receiving data from the peer"）。今日用 `scp` 单文件传输绕过 + 多次重试 fetch 才同步成功。**建议接班后第一件事**：确认服务器 git HEAD 与 origin/master 完全一致。
2. **孤儿文件重现**：服务器工作目录的未跟踪文件（TagManager.tsx、ChainBuilder 等）反复出现，导致 build 失败。每次部署需 `git clean -fd`。今日已清理，下一次 fetch 不需要再清。

## 待办（后续方向）

- **响应式/移动端适配**（桌面端完善，手机端待调）
- **题库批量出题**：当前一次最多 20 题；后续若题库要扩展到几百题，需要分批 + 进度展示
- **生成器准确度提升**：现有技巧库仅到 boxLineReduction（中等），困难级别靠启发式判断；后续若实现 X-Wing/Swordfish 等深度技巧可显著提升评级准确度
- **nginx 反代 + HTTPS**（目前公网直连 3003 端口）

## 接班命令

```bash
cd /Users/lh/Developer/MySudoku
npm run dev
# 游戏：http://localhost:3003/game
# 题库维护：http://localhost:3003/puzzles（自动免密进入维护模式）
# 服务器地址：http://110.42.225.37:3003  密码：Kili+2016
# 服务器 seq=1..8 共 8 道题
```
