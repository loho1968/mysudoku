# 下班交接 -- 2026-07-20

## 今日主题：功能完善 + 发布上线 + 多项改进修复

### 今日完成

#### 一、功能开发（共 8 个 commit，从 `136d77b` 到 `03613ae`）

| # | Commit | 内容 |
|---|--------|------|
| 1 | `2c3784c` | **差错检查改为「提交」按钮**：未填满禁提交、有错仅提示、无错判完成 + 锁棋盘 + 停表 + 首次写 records API + 弹 Modal | 7+ 文件 |
| 2 | `0d67c17` | **CORS 中间件**：修复浏览器跨域同步失败（localhost 白名单、OPTIONS 预检头） | 1 新文件 |
| 3 | `0eeedff` | **全局移除 source + tags 系统**：删除 tags API 路由(3)、删除 TagManager、PuzzleForm/PuzzleTable/SyncBar 去 source/tags、数据库 schema 简化 | 15 文件 |
| 4 | `6fcc10d` | 修复：密码框不再暴露默认密码（`Kili+2016` → `输入编辑密码`）；Space `direction` → `orientation` | 2 文件 |
| 5 | `5132a47` | **粘贴/保存题目按钮**：游戏页新增粘贴按钮（读剪贴板 81 字符+验证加载）、保存按钮（需编辑模式/密码、录入难度备注技巧、POST 入库） | 1 文件 |
| 6 | `5a1cc42` | **技巧选择重构**：返回技巧+题数、每次打开刷新、按钮单选直接出题 | 2 文件 |
| 7 | 布局修复 × 2 | 技巧跳转布局重叠、粘贴按钮不显示的修复 | 3 文件 |
| 8 | `03613ae` | 候选数同数字高亮框 + 技巧名称在计时栏后显示 + hydration 错误修复 | 7 文件 |

#### 二、部署上线（腾讯云 PM2）

- 服务器 `/opt/mysudoku` 对齐 `origin/master`（commit `03613ae`）
- pm2 `mysudoku` 进程在线（id=54, uptime 48m, restart=18）
- 监听 `localhost:3003`，公网 `http://110.42.225.37:3003`

#### 三、踩坑/状态记录

1. **服务器 git 间歇网络故障**：`git fetch` 有时报 `Failure when receiving data from the peer`（GitHub 连接问题），需重试。已多次出现，建议排查或换 SSH 协议拉取。
2. **密码不一致**：服务器 DB `edit_password` = `mysudoku`（旧默认值），已改为 `Kili+2016`；本地新建库默认也是 `Kili+2016`。
3. **CORS 白名单**：仅 localhost/127.0.0.1 可跨域访问 `/api/*`，部署环境无影响。
4. **同步测试验证通过**：本机→服务器（2 题上传）和服务器→本机（拉取）双向测试通过。

## 服务器状态

- **PM2**：mysudoku (id=54) online, 端口 3003
- **数据库**：`/opt/mysudoku/data/mysudoku.db` 完好（密码 `Kili+2016`）
- **代码版本**：`03613ae`（与 origin/master 一致）
- **nginx**：未配置 mysudoku 反代，公网直连 3003

## 待办（后续方向）

- **nginx 反代 + HTTPS**：推荐配独立子域名或子路径，避免公网暴露 3003 端口
- **响应式/移动端**：桌面端完善，手机端待调
- **游戏完成检测 + 自动提交记录**（已有 records API，前端已接入提交流程）
- **题库数据填充**（目前只有 2 道题）
- 同步策略增强：`_modified` 增量同步

## 接班命令

```bash
cd /Users/lh/Developer/MySudoku
npm run dev
# 游戏：http://localhost:3003/game
# 题库维护：http://localhost:3003/puzzles（免密进入）
# 服务器地址：http://110.42.225.37:3003  密码：Kili+2016
```
