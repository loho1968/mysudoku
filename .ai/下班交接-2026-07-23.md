# 下班交接 -- 2026-07-23

## 今日主题：basePath 子路径部署 + nginx 反代 + 备案信息

### 今日完成

#### 一、子路径部署 `/sudoku`（commit `d928e3e`）

**next.config.ts：** 加 `basePath: '/sudoku'`

**新增 `src/config/runtime.ts`：**
- `BASE_PATH` + `api()` 工具，所有手写 `fetch("/api/...")` 统一加前缀

**改造所有手写 fetch + window.location（约 15 个文件）：**
| 文件 | 改动 |
|------|------|
| `useEditMode.ts` | `fetch(AUTH_URL)` → `api(AUTH_URL)` |
| `EditModeModal.tsx` | `fetch("/api/auth")` → `api(...)` |
| `game/page.tsx` | `fetchRandomPuzzle` 的 url、`fetch(/api/puzzles/)`、`apiFetch` |
| `game/[id]/page.tsx` | `fetch` + `window.location.href` |
| `TechniquePicker.tsx` | 2 处 fetch |
| `CacheWarmer.tsx` | `fetch("/api/puzzles/all")` |
| `SyncBar.tsx` | 本地 fetch + apiFetch |
| `PuzzleTable.tsx` | fetch + apiFetch × 3 |
| `PuzzleImportModal.tsx` | `apiFetch` |
| `PuzzleForm.tsx` | apiFetch 的 url |
| `gameRecord.ts` | `fetch` |

**nginx 配置（`/etc/nginx/nginx.conf`）：**
- HTTPS（443）和 HTTP（80）双 server 块均加入：
  ```
  location /sudoku {
      proxy_pass http://127.0.0.1:3003;
  }
  ```
- nginx -t 验证通过后 reload

**PWA 图标：** 补 `public/icons/icon-192.png` + `icon-512.png`（Python 生成）
**manifest.json：** start_url/scope/icons.src 加 `/sudoku/` 前缀
**layout.tsx：** `manifest` 改为 `/sudoku/manifest.json`

#### 二、备案信息（commit `08a4834`）

**AppFooter.tsx 重写：** 居中显示三项信息：
- 网站名称：**loho的个人小站**
- 工信部备案号：**渝ICP备2026016277号-1**（可点击跳转 `https://beian.miit.gov.cn/`）
- 公安备案号：**渝公网安备50019002505557号**（带 PoliceBadge SVG 图标）

样式：小字、浅色、与整体页面色调一致，Space wrap 在移动端自动换行。

### 当前状态

- **本地**：master `08a4834`，工作树干净
- **服务器**：HEAD `08a4834`，build 通过，pm2 在线（id=54, uptime 27m, restart=111）
- **官方域名**：`https://www.luohong.site/sudoku/game`
- **题库**：17 题
- **旧地址**：`http://110.42.225.37:3003` 仍可用

### 部署踩坑

1. **GitHub fetch**：数次超时后重试成功（重试模式已稳定）
2. **nginx 配置**：`proxy_pass` 不带尾部斜杠，让 `/sudoku` 前缀完整透传到 Next.js（因为 basePath 已设置为 `/sudoku`）
3. **basePath 对开发的影响**：`npm run dev` 后访问 `http://localhost:3003/sudoku/game`；所有测试 URL 加了 `/sudoku` 前缀

## 待办（后续方向）

- **离线提交队列**：当前离线提交 records 失败静默丢弃
- **题库扩展**：生成器就绪，可按需批量出题
- **响应式微调**：小屏体验继续优化
- **生成器准确度**：现有技巧库仅到 boxLineReduction

## 接班命令

```bash
cd /Users/lh/Developer/MySudoku
npm run dev
# 访问：http://localhost:3003/sudoku/game
# 线上：https://www.luohong.site/sudoku/game
# 服务器密码：Kili+2016
```
