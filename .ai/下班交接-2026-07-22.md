# 下班交接 -- 2026-07-22

## 今日主题：PWA 离线 + 手机布局优化 + 候选数校验反馈

### 今日完成

#### 一、PWA 离线支持（commit `8cfe825`）

- 新增 `src/lib/puzzleCache.ts`：localStorage 全量缓存题库（去重 + 按 seq 排序）
- 新增 `src/components/Layout/CacheWarmer.tsx`：应用启动静默拉取 `/api/puzzles/all` 写入缓存
- game/page.tsx：`fetchPuzzleById` / `fetchRandomPuzzle` 网络失败时回退到本地缓存
- AppHeader 加在线/离线指示（Wifi/断连图标 + 事件监听）
- 难度选择器离线时友好提示
- 新增常量 `PUZZLES_CACHE_KEY`

#### 二、手机布局 + 候选数校验（commit `7e7eaf0`）

- **AppHeader 去掉「mysudoku」标题**，腾出空间
- **GameToolBar 重构为单行操作栏**：
  - 粘贴（图标） / 撤销（图标） / 橡皮擦（图标） / 提交（图标+文字） / 笔记开关（Switch）
  - 移除原 2x2 网格 + Segmented 模式切换
  - 移除「智能笔记」按钮（固定使用智能笔记）
- **NumberPad 移除橡皮擦**（合并到 toolbar）
- **候选数校验 + 错误反馈**：
  - 开启笔记显示时，答题输入非候选数 → 拒绝输入
  - 桌面端：数字按钮红色闪烁（0.4s shake 动画） + message 提示
  - 移动端：闪烁 + 震动 80ms（`navigator.vibrate`） + 提示
  - 桌面键盘 1-9 同样校验
- CSS：`.toolbar-actions-row` 单行 flex 布局；`@keyframes shake-num` 动画；`.game-panel-meta` 题号行
- game/page.tsx：粘贴按钮移到 GameToolBar（通过 `onPaste` prop 传入），保存按钮独立显示

### 当前状态

- **本地**：master `7e7eaf0`，工作树干净，已推送
- **服务器**：HEAD 同步 `7e7eaf0`，build 通过，pm2 在线（id=54, uptime 6h, restart=109）
- **题库**：17 题
- **公网**：`http://110.42.225.37:3003`

### 部署踩坑

1. **GitHub fetch 间歇失败**：服务器到 GitHub 网络持续不稳，今日通过 fetch 重试（最多 3 次）成功。已是稳定模式，接班后请先确认服务器 HEAD。
2. **手机 PWA 安装**：HTTP 协议下部分 Android 浏览器允许安装 PWA；iOS Safari 对 HTTP PWA 支持有限，如需 iOS 完整支持需配 HTTPS。

## 待办（后续方向）

- **HTTPS + 域名**：让 iOS PWA 安装可用；目前 HTTP 直连 3003 端口
- **题库扩展**：生成器已就绪，可按需批量出题
- **响应式微调**：手机端布局基本可用，可进一步优化小屏（<375px）体验
- **离线提交队列**：当前离线提交 records 失败静默丢弃，后续可加队列联网后补提交
- **生成器准确度**：现有技巧库仅到 boxLineReduction，困难级别靠启发式

## 接班命令

```bash
cd /Users/lh/Developer/MySudoku
npm run dev
# 游戏：http://localhost:3003/game
# 题库维护：http://localhost:3003/puzzles（自动免密）
# 服务器：http://110.42.225.37:3003  密码：Kili+2016
```
