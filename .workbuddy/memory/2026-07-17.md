# 2026-07-17 工作日志

- 通过 `ssh tencent` 连接远程服务器，项目目录 `/opt/mysudoku`，摸清为 Next.js 16 数独 PWA 项目结构。
- 记录项目规则：代码搜索优先用 CodeGraph，每次改代码后执行 `codegraph sync`。（写入 MEMORY.md）
- 决策：MySudoku 保持在 `/opt/mysudoku`，不迁移到 `/developer`。理由：`/opt` 已是 mycube/roamly/harmony-and-mac 等服务器开发项目的家目录，保持一致且避免改 ecosystem.config.js 的 4 处硬编码路径。
- 部署落地：
  - 创建 `.ai/deploy.sh`（发布：source nvm → build → pm2 reload/start → pm2 save）与 `.ai/handover.sh`（交班：先发布再 git add -A/commit/push -u origin master）。
  - 踩坑1：node/pm2 在 nvm 下，非登录 shell 无 PATH，脚本须先 source nvm。
  - 踩坑2：`next build` 因服务器仅 3.7GB 内存且无 swap 被 OOM 杀(exit 137)；已加 4GB `/swapfile` 并写入 `/etc/fstab` 持久化。
  - 修复代码 bug：`src/lib/sudoku/techniques/index.ts` 引用了不存在的 `./boxLineReduction`，已补建 `boxLineReduction.ts`（区块删减法 Claiming）。
  - 改代码后执行了 `npm run s:sync`（codegraph 同步）。
  - 结果：构建通过，mysudoku 已在 pm2 运行(id 54)，`/game`、`/puzzles`、`/api/puzzles`、`/api/tags` 均 200，better-sqlite3 正常。改动尚未 commit（等"交班"）。
- Q1 配置：把 nvm 下的 node/npm/npx/pm2 软链到 /usr/local/bin，使任意 shell/项目无需 source nvm 即可用 pm2（已验证 `pm2 --version`=7.0.3、`pm2 list` 可见守护进程）。
- Q2 讨论：用户质疑"服务器直接开发"是否必要（build 有内存隐患）。初步结论：当前已用 4GB swap 顶住；长期看与用户既有的"本机开发→发布到 opt"(Roamly 模式)不一致，且本地机器内存更充裕、构建更稳。待用户决定长期开发模型。
- **开发模型切换为 Roamly 模式（本机开发 + 一键发布）**：
  - 本地克隆仓库到 /Users/lh/WorkBuddy/MySudoku（从服务器 clone，保留 .workbuddy）；origin 设为 GitHub(https，本机 osxkeychain 存了 loho1968 凭据；SSH key 未被 GitHub 授权故不用 SSH)；服务器 node 24.18.0 软链到 /usr/local/bin。
  - 先 `git commit` 服务器未提交改动(a807cbb：补 boxLineReduction、发布/交班脚本、.gitignore 等)再克隆。
  - 本机 `npm install`(701 包) + `npm run build` 成功(8.5s，无 OOM)，验证本机构建可行。
  - 重写 `.ai/deploy.sh`(本机)：本机 build → rsync(排除 node_modules/.git/data/logs/.codegraph/.workbuddy/.next/cache) → 服务器 `npm install` + `pm2 reload`。`.ai/handover.sh`(本机)：commit+push GitHub → deploy。
  - 服务器 `.ai/deploy.sh` 改为仅重启(避免再 OOM)。
  - 实跑发布验证：本机 build 3.4s → rsync → pm2 reload 成功 → 线上 /game /puzzles /api/puzzles /api/tags 均 200。
- **本机项目目录迁移**：从 `/Users/lh/WorkBuddy/MySudoku` 移动到 `/Users/lh/Developer/MySudoku`（用户要求）。`.workbuddy` 记忆目录一并搬走；git 完好(origin=GitHub)；`.ai` 脚本用 `cd "$(dirname "$0")/.."` 定位根，移动后无需改脚本（仅 deploy.sh 第 8 行引用系统 node 路径 `/Users/lh/.nvm/...`，不受影响）。已更新 MEMORY.md 本地路径。
